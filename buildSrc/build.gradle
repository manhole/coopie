plugins {
    id 'groovy-gradle-plugin'
}

gradlePlugin {
    plugins {
        buildNumber {
            id = "com.tdder.gradle.buildNumber"
            implementationClass = "com.tdder.gradle.BuildNumberPlugin"
        }
    }
}
